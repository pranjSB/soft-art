name: Playwright Tests

on:
  pull_request:
    branches: [ main, master ]
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    timeout-minutes: 60

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: lts/*

      - name: Install dependencies
        run: npm ci

      - name: Install Playwright browsers
        run: npx playwright install --with-deps

      - name: Run Playwright tests
        run: npx playwright test
        env:
          CI: true
          BASE_URL: ${{ secrets.BASE_URL }}
          SIGNIN_URL: ${{ secrets.SIGNIN_URL }}
          CREATE_ACCOUNT_URL: ${{ secrets.CREATE_ACCOUNT_URL }}
          SIGNIN_EMAIL: ${{ secrets.SIGNIN_EMAIL }}
          SIGNIN_PASSWORD: ${{ secrets.SIGNIN_PASSWORD }}
          FIRST_NAME: ${{ secrets.FIRST_NAME }}
          LAST_NAME: ${{ secrets.LAST_NAME }}
          PHONE: ${{ secrets.PHONE }}

      - name: Upload console error logs
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: playwright-console-errors
          path: logs/console-errors.json
          retention-days: 30

      - name: Upload Playwright test artifacts
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: playwright-test-artifacts
          path: |
            test-results/
            playwright-report/
          retention-days: 14

      - name: Upload Playwright HTML report
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: playwright-html-report
          path: playwright-report/
          retention-days: 30
